<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/header_height"
    android:orientation="vertical"
    tools:context=".MainActivity" 
    android:background="@color/header_color"
    >
    <!-- Header -->
    	<ImageView
	  	    android:id="@+id/logo"
	  	    android:layout_width="wrap_content"
	  	    android:layout_height="@dimen/header_height"
	  	    android:layout_alignParentLeft="true"
	  	    android:layout_alignParentTop="true"
	  	    android:layout_marginLeft="20dp"
	  	    android:gravity="center"
	  	    android:src="@drawable/help" 
	  	/>
		<TableRow
	  	    android:id="@+id/help_menu"
	  	    android:layout_width="wrap_content"
	  	    android:layout_height="match_parent"
	  	    android:layout_alignParentTop="true"
	  	    android:layout_toRightOf="@+id/logo"
	  	    android:layout_marginLeft="80dip"
	  	    android:layout_marginRight="30dip"
	  	    android:gravity="center">

	  	    <ImageView
	  	        android:id="@+id/help_logo"
	  	        android:layout_weight="1"
	  	        android:contentDescription="@string/help_description"
	  	        android:src="@drawable/ic_help" 
	  	        android:layout_width="25dip"
	  	        />
	  	    <TextView
	  	        android:id="@+id/text_help_logo"
	  	        android:textColor="@color/header_textColor"
	  	        android:textSize="@dimen/header_help_logo_textSize"
	  	        android:layout_marginLeft="3dip"
	  	        android:layout_width="wrap_content"
	  	        android:layout_height="match_parent"
	  	        android:layout_weight="1"
	  	        android:gravity="center"
	  	        android:text="Help" />
	  	</TableRow>
	  	
	  	<Spinner
	  	    android:id="@+id/spinner_locations"
	  	    android:layout_width="wrap_content"
	  	    android:layout_height="match_parent"
	  	    android:layout_toRightOf="@+id/help_menu" 
	  		android:gravity="center_vertical"   
	  	/>

    <!-- end header -->
</RelativeLayout>